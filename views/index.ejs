<!--Update-->
<!DOCTYPE html>
<html>
  <head>
    <title>Contact AK</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="icon" href="/images/chat.png" />
  </head>
  <body>
    <center>
      <br>
      <span>♕</span>
      <h1>Contact AK</h1>
    <form method="post" action="/submit">
      <div>
      <input type="text" id="username" name="username" minlength="5" maxlength="30" spellcheck="false" placeholder="Full Name" required><br><br>
      <input type="email" id="email" name="email" spellcheck="false" placeholder="Email" required><br><br>
      <textarea id="message" name="message" minlength="10" placeholder="Message" required></textarea>
      </div><br><br>
      <input type="submit" value="Submit">
    </form>
    </center>
    <style>
input[type="text"] {
  height: 48px;
  width: 280px;
  border: 1px solid #c0c0c0;
  border-radius: 6px;
  box-sizing: border-box;
  padding: 16px;
}

input[type="email"] {
  height: 48px;
  width: 280px;
  border: 1px solid #c0c0c0;
  border-radius: 6px;
  box-sizing: border-box;
  padding: 16px;
  font-family: 'Arial Rounded MT';
}

input[type="text"] {
  font-size: 16px;
}

input[type="email"] {
  font-size: 16px;
}

input[type="text"]:focus {
  outline: none;
  border: 2px solid blue;
}

input[type="email"]:focus {
  outline: none;
  border: 2px solid blue;
}

input[type="text"]:focus + .label .text, :not(input[value=""]) + .label .text {
  font-size: 12px;
  transform: translate(0, -150%);
  background-color: white;
  padding-left: 4px;
  padding-right: 4px;
}

input[type="email"]:focus + .label .text, :not(input[value=""]) + .label .text {
  font-size: 12px;
  transform: translate(0, -150%);
  background-color: white;
  padding-left: 4px;
  padding-right: 4px;
}

input[type="text"]:focus + .label .text {
  color: blue;
}
input[type="email"]:focus + .label .text {
  color: blue;
}
    </style>
  </body>
</html>
